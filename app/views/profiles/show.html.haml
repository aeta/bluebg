= render 'userinfo'

- if @user.posts.count > 0
  -@posts.each do |post|
    = render 'posts/post', post: post
- else
  .text-center.lightgray
    %h2 This user hasn't posted anything
    %br
    .text200
      %span.fa.fa-frown-o
